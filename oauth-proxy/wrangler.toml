name = "bjj-dojo-oauth-proxy"
main = "worker.ts"
compatibility_date = "2025-01-01"

# Deployed automatically by .github/workflows/deploy-oauth-proxy.yml
# whenever this folder changes (or on manual workflow_dispatch).
#
# The deployed URL (e.g. https://bjj-dojo-oauth-proxy.<account>.workers.dev)
# is baked into the PWA build via the VITE_GITHUB_OAUTH_PROXY_URL repo
# secret in the deploy.yml workflow.
