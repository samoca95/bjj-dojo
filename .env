# Public OAuth App client_id for GitHub Device Flow. Safe to commit — Device
# Flow client_ids are not secrets. Override per build by setting the env var
# in CI or a local .env.local file.
VITE_GITHUB_OAUTH_CLIENT_ID=Ov23liuC5NwCTSZfBnuu
